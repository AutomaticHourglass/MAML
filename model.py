# General collecting part of all the models

import model_segnet
import model_danet

class SSegModel():
    def __init__(self,model_name,model_params):
        if(model_name == 'unet'):
            
        pass

